# LeashOff — 画面実装 引き継ぎメモ（Claude Code用）

最終更新: 2026-08-16

このメモは、画面とナビゲーションの実装だけをスコープにしたものです。
RevenueCat課金・API連携は含みません（別タスクで着手）。

---

## 1. アプリ概要

散歩写真からリードを自動検出・除去し、プリント用高解像度画像を書き出すiOSアプリ。

- Bundle ID: `com.yamatohoriguchi.leashoff`
- 技術スタック: Expo (SDK 57) / React Native
- 対象: iOS単独

---

## 2. 画面フロー（確定版）

```
[F-01 写真選択]
      ↓
[F-02 自動検出]
      ↓
   検出できた？
   ├─ No → [検出失敗画面] （F-10、クレジット消費なし）
   │         └─ 手動補正へ誘導 or 写真選び直し
   └─ Yes ↓
[F-03 検出結果の補正]
   （ハイライトの追加/除外のみ。首輪トグルは廃止済み・実装不要）
      ↓
[F-05 除去実行]（サーバー処理、進捗表示）
      ↓
[F-06 ビフォー/アフター比較]（スライダー or 長押し）
   ├─ 「やり直す」→ F-03 に戻る
   └─ 次へ ↓
[F-07/F-08 書き出し選択]（標準 / プリント）
   ├─ 「やり直す」→ F-03 に戻る
   ├─ 標準を選択 → 即書き出し（無料、クレジット判定なし）
   └─ プリントを選択 → クレジット確認（F-09）
         ├─ 残クレジットあり → 書き出し実行
         └─ 不足 → 購入画面へ → 購入後、書き出し選択に戻る
```

- F-11（プライバシー表示）は初回起動時のオンボーディングに配置。メイン導線には挟まない。
- クレジット消費タイミングは「プリント書き出し時」のみ（B方針で確定）。除去処理（F-02〜F-06）は何度でも無料で試せる。
- 除去処理自体の乱用防止（1日あたりの回数制限など）は別途検討中。原価データ待ちのため今回のスコープ外。

---

## 3. 画面ごとの要素

### F-01 写真選択
- カメラロールから1枚選択（複数選択は対象外）

### F-02 自動検出
- 選択後、自動でリードをハイライト表示
- 検出失敗時は分岐（上記フロー参照）

### F-03 検出結果の補正
- ハイライトされた検出結果を指で追加/除外
- 首輪の扱いに関するUIは**不要**（常に首輪は残す仕様に統一済み）
- 「除去を実行」ボタンで次へ

### F-05 除去実行
- サーバー処理中の進捗表示（ローディング状態）

### F-06 ビフォー/アフター比較
- スライダー、または長押しで元画像表示
- 「やり直す」ボタン → F-03へ

### F-07/F-08 書き出し選択
- 「標準書き出し」（無料・SNS向け解像度）
- 「プリント書き出し」（有料クレジット・縦横比プリセット：正方形/A4/横長）
- 残クレジット数を表示
- 「やり直す」ボタン → F-03へ

### クレジット購入画面（F-09関連）
- プリント書き出し選択時、クレジット不足なら遷移
- 購入後は書き出し選択画面に戻す（フローの最初に戻さない）

---

## 4. デザイントークン

コンセプト：「散歩道の素材感」— リードの素材（革・ロープ・真鍮）を意匠に転用。
AI系アプリにありがちな「クリーム×テラコッタ」「漆黒×蛍光」は避ける。

### カラー
| 用途 | HEX |
|---|---|
| 基調（フォレストグリーン） | `#2B3A2E` |
| 基調・濃 | `#1E2921` |
| 背景（キャンバス生成り） | `#E8E1D3` |
| 背景・明 | `#F1ECE1` |
| レザータン | `#8B5E3C` |
| 真鍮アクセント | `#B08D57` |
| 真鍮アクセント・明 | `#C7A876` |
| 本文（ほぼ黒） | `#211F1B` |

### タイポグラフィ
- ディスプレイ（見出し・ロゴ）: セリフ体、やや武骨な太さ（例: Georgia系）。多用しない
- 本文: ヒューマニストサンセリフ、可読性優先
- 数字（クレジット残数・解像度など）: 等幅寄り

### 装飾モチーフについて
- バックル（差し込み式留め具）のビジュアルは検討中。元々は首輪トグルの信号要素だったが、
  首輪トグル自体が廃止されたため、**今回の実装には含めない**。装飾として復活させる場合は別途指示する。

---

## 5. 今回のスコープ

**含む:**
- 画面遷移（ナビゲーション構造一式）
- 各画面の静的UI（上記デザイントークン準拠）
- 分岐ロジックの骨格（検出失敗、クレジット不足の分岐 — ただし実データではなくモック状態でOK）

**含まない（別タスク）:**
- RevenueCat SDK実装・実際の課金処理
- 画像処理APIとの実通信（公開されたAPIへの接続は次段階）
- クレジット残高の永続化ロジック
- App Store審査関連の設定
